# Nixpacks config — Railway uses this to build AND run the app
# Custom server.js runs Next.js + Socket.io on the SAME port (Railway requirement)

[phases.setup]
nixPkgs = ["nodejs_20", "bun"]
aptPkgs = ["openssl", "libssl3", "ca-certificates", "curl"]

[phases.install]
cmds = [
  "bun install",
  "cd mini-services/otc-engine && bun install && cd ../..",
  "bunx prisma generate"
]

[phases.build]
cmds = [
  "mkdir -p /app/db",
  "NEXT_TELEMETRY_DISABLED=1 NODE_OPTIONS='--max-old-space-size=512' npx next build"
]

[start]
# Run custom server.js with Node.js (NOT Bun — Bun crashes with stack smashing)
cmd = "bash railway-start.sh"
