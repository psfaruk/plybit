# Railway config — force Nixpacks (not Dockerfile) for faster builds
# Dockerfile multi-stage takes too long; Nixpacks is optimized for Railway

[build]
builder = "nixpacks"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
